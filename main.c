#include <stdio.h>
#include <stdlib.h>

int main()
{
    int first ,secand,sum;
    printf("Enter two no \n");
    scanf("%d%d",&first,&secand);
    sum=first+secand;
    printf("sum of two no is = %d",sum);
    return 0;
}
